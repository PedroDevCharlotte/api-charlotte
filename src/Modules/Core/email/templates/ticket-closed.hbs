<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>{{subject}}</title>
  <style>
    body{margin:0;padding:0;font-family:Segoe UI, Tahoma, Geneva, Verdana, sans-serif;background:#f8f9fa;color:#333}
    .container{max-width:800px;margin:20px auto;background:#fff;border-radius:8px;overflow:hidden;box-shadow:0 8px 24px rgba(0,0,0,0.06)}
    .header{padding:24px;text-align:center;background:linear-gradient(135deg,#6c757d,#495057);color:white}
    .logo{max-width:200px;height:auto;margin-bottom:8px}
    .content{padding:28px}
    .footer{padding:18px;text-align:center;background:#f8f9fa;color:#6c757d;font-size:12px}
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      {{#if systemInfo.logoData}}<img src="{{systemInfo.logoData}}" class="logo" alt="Charlotte" />{{else}}<img src="{{systemInfo.logoUrl}}" class="logo" alt="Charlotte" />{{/if}}
      <h2 style="margin:0">Ticket cerrado</h2>
    </div>
    <div class="content">
      {{#if content}}<div style="margin-bottom:16px">{{{content}}}</div>{{/if}}

      <div style="background:linear-gradient(135deg,#28a745,#1e7e34);color:white;padding:24px;border-radius:8px;text-align:center;margin-bottom:20px">
        <div style="font-size:56px">✅</div>
        <h3 style="margin:8px 0 0 0;font-size:22px">Ticket Completado</h3>
        <p style="margin:8px 0 0 0;opacity:0.95">El ticket ha sido resuelto y cerrado exitosamente</p>
      </div>

      <div style="padding:18px;background:#fff;border-radius:8px;border:1px solid #e9ecef;margin-bottom:18px">
        <h1 style="margin:0 0 12px 0;font-size:20px;color:#495057;text-align:center">{{ticket.title}}</h1>
        <div style="display:flex;justify-content:center;gap:12px;flex-wrap:wrap;margin-bottom:16px">
          <span style="background:#f8f9fa;border:1px solid #e9ecef;padding:6px 12px;border-radius:16px;font-size:12px;color:#6c757d">#{{ticket.ticketNumber}}</span>
          <span style="background:{{ticket.priorityColor}};color:white;padding:6px 12px;border-radius:16px;font-size:12px">{{ticket.priority}}</span>
          <span style="background:#6c757d;color:white;padding:6px 12px;border-radius:16px;font-size:12px">CERRADO</span>
        </div>

        <div style="background:#f8f9fa;padding:14px;border-radius:8px;border-left:4px solid #28a745;margin-bottom:14px">
          <div style="font-weight:600;color:#155724">Cerrado por:</div>
          <div style="color:#28a745;font-size:16px;font-weight:600">{{user.fullName}}</div>
          <div style="color:#6c757d;font-size:13px">{{user.email}}</div>
        </div>

        {{#if customMessage}}<div style="background:#e7f3ff;padding:12px;border-radius:6px;margin-bottom:12px">{{breaklines customMessage}}</div>{{/if}}

        {{#if ticket.resolution}}<div style="margin-top:12px;background:#d4edda;padding:12px;border-radius:6px;border-left:4px solid #28a745">{{breaklines ticket.resolution}}</div>{{/if}}

        <div style="text-align:center;margin-top:16px"><a href="{{ticket.url}}" style="background:#6c757d;color:#fff;padding:10px 18px;border-radius:6px;text-decoration:none;display:inline-block">📋 Ver Historial Completo</a></div>
      </div>

      <div style="background:#fff;border:1px solid #e9ecef;border-radius:8px;padding:16px;margin-bottom:18px">
        <h4 style="margin:0 0 8px 0;color:#495057">Resumen rápido</h4>
        <p style="margin:0 0 8px 0;color:#6c757d">#{{ticket.ticketNumber}} • {{ticket.title}}</p>
        <p style="margin:0 0 12px 0;color:#6c757d">Estado: <strong style="color:{{ticket.statusColor}}">{{ticket.status}}</strong> — Prioridad: <strong style="color:{{ticket.priorityColor}}">{{ticket.priority}}</strong></p>
      </div>

      {{#unless isCopy}}
      <div style="background:#d4edda;border:1px solid #c3e6cb;border-radius:8px;padding:18px;margin-bottom:18px">
        <h3 style="margin:0 0 8px 0;color:#155724">🎉 ¡Ticket completado exitosamente!</h3>
        <ul style="margin:8px 0 0 20px;color:#155724">
          <li>Tu problema ha sido resuelto satisfactoriamente</li>
          <li>Toda la información queda registrada en el sistema</li>
        </ul>
      </div>
      {{/unless}}
    </div>
    <div class="footer">Este ticket fue cerrado el {{ticket.formattedClosedAt}}. • Sistema de Tickets • Charlotte Chemical Internacional • {{systemInfo.year}}</div>
  </div>
</body>
</html>

