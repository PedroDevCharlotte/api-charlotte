<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>{{subject}}</title>
  <style>
    body{margin:0;padding:0;font-family:Segoe UI, Tahoma, Geneva, Verdana, sans-serif;background:#f8f9fa;color:#333}
    .container{max-width:600px;margin:20px auto;background:#fff;border-radius:6px;overflow:hidden;box-shadow:0 6px 18px rgba(0,0,0,0.06)}
    .header{padding:20px;text-align:center;background:linear-gradient(135deg,#28a745,#1e7e34);color:white}
    .logo{max-width:180px;height:auto;margin-bottom:8px}
    .content{padding:28px}
    .footer{padding:18px;text-align:center;background:#f8f9fa;color:#6c757d;font-size:12px}
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      {{#if systemInfo.logoData}}<img src="{{systemInfo.logoData}}" class="logo" alt="Charlotte" />{{else}}<img src="{{systemInfo.logoUrl}}" class="logo" alt="Charlotte" />{{/if}}
      <h2 style="margin:0">Ticket Asignado</h2>
    </div>
    <div class="content">
      {{#if content}}<div style="margin-bottom:16px">{{{content}}}</div>{{/if}}

      <div style="background:#f8f9fa;padding:20px;border-radius:8px;margin-bottom:20px;border:1px solid #e9ecef">
        <div style="display:flex;align-items:center;gap:12px">
          <div style="width:48px;height:48px;border-radius:50%;background:#28a745;display:flex;align-items:center;justify-content:center;color:#fff;font-size:20px">👤</div>
          <div>
              <div style="font-weight:600;color:#28a745">{{#if isCopy}}CC: {{/if}}Ticket asignado</div>
              <div style="color:#6c757d;font-size:13px">#{{ticket.ticketNumber}} • Asignado por {{user.firstName}} {{user.lastName}}</div>
          </div>
        </div>
      </div>

      <div style="background:#fff;border:1px solid #e9ecef;border-radius:8px;padding:18px;margin-bottom:18px">
        {{#if ticket.assignee}}
          <div style="text-align:center;margin-bottom:12px">
              <h3 style="margin:0">{{ticket.assignee.firstName}} {{ticket.assignee.lastName}}</h3>
              <div style="color:#386641">{{ticket.assignee.email}}</div>
          </div>
        {{/if}}
        {{#if customMessage}}<div style="background:#e7f3ff;padding:12px;border-radius:6px;margin-top:12px">{{breaklines customMessage}}</div>{{/if}}
      </div>

      <div style="text-align:center;margin-top:12px">
        <a href="{{ticket.url}}" style="background:#28a745;color:#fff;padding:10px 18px;border-radius:6px;text-decoration:none;display:inline-block">Ver caso</a>
      </div>
    </div>
    <div class="footer">Sistema de Tickets • Charlotte Chemical Internacional • {{systemInfo.year}}</div>
  </div>
</body>
</html>
