<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Advertencia de Expiración de Contraseña</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            margin: 0;
            padding: 0;
            background-color: #f8f9fa;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .header {
            background: linear-gradient(135deg, #007bff 0%, #0056b3 100%);
            color: white;
            padding: 30px 20px;
            text-align: center;
        }
        .header.critical {
            background: linear-gradient(135deg, #dc3545 0%, #c82333 100%);
        }
        .header.urgent {
            background: linear-gradient(135deg, #fd7e14 0%, #e55a00 100%);
        }
        .header.high {
            background: linear-gradient(135deg, #ffc107 0%, #e0a800 100%);
            color: #212529;
        }
        .logo {
            max-width: 200px;
            height: auto;
            margin-bottom: 15px;
        }
        .content {
            padding: 40px 30px;
        }
        .alert-box {
            border-left: 4px solid #007bff;
            background-color: #e7f3ff;
            padding: 20px;
            margin: 20px 0;
            border-radius: 4px;
        }
        .alert-box.critical {
            border-left-color: #dc3545;
            background-color: #f8d7da;
            color: #721c24;
        }
        .alert-box.urgent {
            border-left-color: #fd7e14;
            background-color: #fff3cd;
            color: #856404;
        }
        .alert-box.high {
            border-left-color: #ffc107;
            background-color: #fff3cd;
            color: #856404;
        }
        .days-counter {
            font-size: 48px;
            font-weight: bold;
            text-align: center;
            margin: 20px 0;
            color: #007bff;
        }
        .days-counter.critical {
            color: #dc3545;
        }
        .days-counter.urgent {
            color: #fd7e14;
        }
        .days-counter.high {
            color: #ffc107;
        }
        .btn {
            display: inline-block;
            padding: 12px 24px;
            background-color: #007bff;
            color: white;
            text-decoration: none;
            border-radius: 4px;
            font-weight: bold;
            text-align: center;
            margin: 20px 0;
        }
        .btn.critical {
            background-color: #dc3545;
            animation: pulse 2s infinite;
        }
        .btn.urgent {
            background-color: #fd7e14;
        }
        .btn.high {
            background-color: #ffc107;
            color: #212529;
        }
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }
        .btn:hover {
            opacity: 0.9;
            transform: translateY(-1px);
        }
        .security-tips {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 4px;
            margin: 20px 0;
        }
        .footer {
            background-color: #f8f9fa;
            padding: 20px;
            text-align: center;
            color: #6c757d;
            font-size: 14px;
            border-top: 1px solid #dee2e6;
        }
        .footer a {
            color: #007bff;
            text-decoration: none;
        }
        ul {
            margin: 10px 0;
            padding-left: 20px;
        }
        li {
            margin: 5px 0;
        }
        .icon {
            font-size: 24px;
            margin-right: 10px;
        }
        @media (max-width: 600px) {
            .content {
                padding: 20px 15px;
            }
            .days-counter {
                font-size: 36px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header {{urgencyLevel}}">
            <img src="{{logoUrl}}" alt="Charlotte Chemical" class="logo">
            <h1>
                {{#if isExpired}}
                    🚨 Contraseña Vencida
                {{else if isExpiringTomorrow}}
                    ⚠️ Contraseña Vence Mañana
                {{else}}
                    ⏰ Contraseña Próxima a Vencer
                {{/if}}
            </h1>
        </div>

        <div class="content">
            <h2>Hola {{userName}},</h2>
            
            <div class="alert-box {{urgencyLevel}}">
                <p><strong>{{customMessage}}</strong></p>
            </div>

            {{#if isExpired}}
                <div class="days-counter critical">
                    ¡VENCIDA!
                </div>
                <p><strong>Su contraseña ha vencido y debe cambiarla inmediatamente para poder acceder al sistema.</strong></p>
            {{else}}
                <div class="days-counter {{urgencyLevel}}">
                    {{daysRemaining}}
                    {{#if isExpiringTomorrow}}día{{else}}días{{/if}}
                </div>
                <p>Su contraseña vencerá en <strong>{{daysRemaining}} {{#if isExpiringTomorrow}}día{{else}}días{{/if}}</strong>.</p>
            {{/if}}

            <div style="text-align: center; margin: 30px 0;">
                <a href="{{changePasswordUrl}}" class="btn {{urgencyLevel}}">
                    🔐 Cambiar Contraseña Ahora
                </a>
            </div>

            <div class="security-tips">
                <h3><span class="icon">🛡️</span>Consejos de Seguridad</h3>
                <p>Para crear una contraseña segura, asegúrese de que:</p>
                <ul>
                    <li>Tenga al menos 8 caracteres de longitud</li>
                    <li>Incluya mayúsculas y minúsculas</li>
                    <li>Contenga números y símbolos especiales</li>
                    <li>No sea una contraseña que haya usado anteriormente</li>
                    <li>No contenga información personal como nombres o fechas</li>
                </ul>
            </div>

            <p><strong>¿Por qué es importante cambiar mi contraseña?</strong></p>
            <p>Cambiar las contraseñas regularmente es una práctica de seguridad esencial que ayuda a:</p>
            <ul>
                <li>Proteger sus datos personales y de la empresa</li>
                <li>Prevenir accesos no autorizados</li>
                <li>Cumplir con las políticas de seguridad</li>
                <li>Mantener la integridad del sistema</li>
            </ul>

            {{#if (eq urgencyLevel "critical")}}
                <div class="alert-box critical">
                    <p><strong>⚠️ ATENCIÓN:</strong> Su cuenta puede ser bloqueada si no cambia su contraseña inmediatamente.</p>
                </div>
            {{/if}}

            <p>Si tiene problemas para cambiar su contraseña o necesita ayuda, no dude en contactar al departamento de soporte técnico.</p>

            <p>Gracias por su atención y por mantener nuestros sistemas seguros.</p>

            <p>Saludos cordiales,<br>
            <strong>Equipo de Seguridad Informática</strong><br>
            Charlotte Chemical Internacional</p>
        </div>

        <div class="footer">
            <p>© {{year}} Charlotte Chemical Internacional, S.A. de C.V.</p>
            <p>Este es un mensaje automático del sistema de seguridad. Por favor, no responda a este correo.</p>
            <p>Si no solicitó este cambio, contacte inmediatamente al área de sistemas.</p>
            <p><a href="{{loginUrl}}">Acceder al Sistema</a></p>
        </div>
    </div>
</body>
</html>
