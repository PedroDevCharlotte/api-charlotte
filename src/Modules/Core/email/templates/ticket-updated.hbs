<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>{{subject}}</title>
  <style>
    body{margin:0;padding:0;font-family:Segoe UI, Tahoma, Geneva, Verdana, sans-serif;background:#f8f9fa;color:#333}
    .container{max-width:700px;margin:20px auto;background:#fff;border-radius:8px;overflow:hidden;box-shadow:0 8px 20px rgba(0,0,0,0.06)}
    .header{padding:20px;text-align:center;background:linear-gradient(135deg,#17a2b8,#117a8b);color:white}
    .logo{max-width:160px;height:auto;margin-bottom:8px}
    .content{padding:24px}
    .footer{padding:16px;text-align:center;background:#f8f9fa;color:#6c757d;font-size:12px}
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      {{#if systemInfo.logoData}}<img src="{{systemInfo.logoData}}" class="logo" alt="Charlotte" />{{else}}<img src="{{systemInfo.logoUrl}}" class="logo" alt="Charlotte" />{{/if}}
      <h2 style="margin:0">Ticket Actualizado</h2>
    </div>

    <div class="content">
      {{#if content}}<div style="margin-bottom:16px">{{{content}}}</div>{{/if}}

      <div style="background:#fff;border-radius:8px;border:1px solid #e9ecef;padding:18px;margin-bottom:16px">
        <h3 style="margin:0 0 8px 0;color:#17a2b8">Ticket actualizado</h3>
        <p style="margin:0 0 12px 0;color:#6c757d">El ticket ha recibido una actualización por <strong>{{user.fullName}}</strong>.</p>

        <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(160px,1fr));gap:12px;margin-top:12px">
          <div style="background:#f8f9fa;padding:12px;border-radius:8px;text-align:center">
            <strong style="display:block;color:#495057">Ticket</strong>
            <div style="color:#6c757d">#{{ticket.ticketNumber}} • {{ticket.title}}</div>
          </div>

          <div style="background:#f8f9fa;padding:12px;border-radius:8px;text-align:center">
            <strong style="display:block;color:#495057">Prioridad</strong>
            <div style="color:#6c757d">{{ticket.priority}}</div>
          </div>

          <div style="background:#f8f9fa;padding:12px;border-radius:8px;text-align:center">
            <strong style="display:block;color:#495057">Estado</strong>
            <div style="color:#6c757d">{{ticket.status}}</div>
          </div>
        </div>

        <div style="text-align:center;margin-top:18px"><a href="{{ticket.url}}" style="background:#17a2b8;color:white;padding:10px 18px;border-radius:8px;text-decoration:none;display:inline-block">Ver ticket</a></div>
      </div>

      <div style="background:#fff;border:1px solid #e9ecef;border-radius:8px;padding:16px;margin-bottom:18px">
        <h4 style="margin:0 0 8px 0;color:#495057">Detalles</h4>
        {{#if previousValues}}
          <div style="margin-bottom:12px;color:#6c757d">Se registraron cambios importantes en el ticket.</div>
        {{/if}}
        {{#if customMessage}}<div style="background:#e7f3ff;padding:12px;border-radius:6px;margin-top:8px">{{breaklines customMessage}}</div>{{/if}}
      </div>
    </div>

    <div class="footer">Sistema de Tickets • Charlotte Chemical Internacional • {{systemInfo.year}}</div>
  </div>
</body>
</html>
